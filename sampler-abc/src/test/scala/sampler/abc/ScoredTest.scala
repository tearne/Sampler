package sampler.abc

import org.scalatest.FreeSpec

class ScoredTest extends FreeSpec {
  "Scored should" - {
  	"Throw exception if any of the scores are negative" in fail("TODO")
  	"Return number of reps as a double" in fail("TODO")
  }
}