package sampler.abc.actor.root.phase

import org.scalatest.FreeSpec

class IdleTest extends FreeSpec {
  "Idle should" - {
    "Start normally (from model prior)" in {
      fail("TODO")
    }
    "Start from a previous generation" in {
      fail("TODO")
    }
    "Be tested" in {fail("TODO")}
  }
}
